import styled from 'styled-components';
import { ModalFormBox } from '../UI/MyModal/ModalFormStyles';

export const AuctionBox = styled(ModalFormBox)`
  overflow-x: hidden;
  overflow-y: auto;
  padding-bottom: 38px;
`;
